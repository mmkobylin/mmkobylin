import React from 'react'

const Section = ( { message } ) => {

    return (
        <section className="section">
            no kurwa nie moge.
            <p> { message } </p>
        </section>
    )
}

export default Section
